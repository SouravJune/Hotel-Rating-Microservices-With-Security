server:
  port: 8088

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/SouravJune/Hotel-Rating-Microservices-With-Security
          clone-on-start: true